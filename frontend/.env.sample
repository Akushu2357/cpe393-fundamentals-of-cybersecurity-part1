# Sample environment variables for the frontend (Vite)

# Backend base URL used by frontend/src/api.js
VITE_API_BASE=http://localhost:4000

# Optional: Vite dev server port (uncomment to override)
# VITE_DEV_PORT=5173
